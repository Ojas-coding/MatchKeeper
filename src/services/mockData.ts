import { Event, Match, User } from "@/types";

// Users array - starts empty, users register through the app
export const mockUsers: User[] = [];

// Events array - starts empty, events are created through the app
export const mockEvents: Event[] = [];

// Matches array - starts empty, matches are created through the app
export const mockMatches: Match[] = [];
